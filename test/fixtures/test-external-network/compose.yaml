services:
  web:
    image: docker.io/nginx:alpine
    environment:
      NGINX_WORKER_PROCESSES: 1
    networks:
      - br0

networks:
  br0:
    external: true
    x-incus-compose:
      network: incusbr0
