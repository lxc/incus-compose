services:
  app:
    image: docker.io/library/nginx:alpine
    environment:
      NGINX_WORKER_PROCESSES: 1
    volumes:
      - type: volume
        source: appdata
        target: /data/vol

volumes:
  appdata:
