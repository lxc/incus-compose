name: with-external-network

services:
  web:
    image: docker.io/nginx:alpine
    environment:
      NGINX_WORKER_PROCESSES: 1
    networks:
      - named-override

networks:
  named-override:
    external: true
    x-incus-compose:
      network: incusbr0
